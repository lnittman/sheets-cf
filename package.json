{
  "name": "sheets-cf",
  "version": "1.0.0",
  "description": "Developer report generator with deep analysis capabilities",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy-worker": "wrangler deploy",
    "deploy-pages": "wrangler pages deploy dist --project-name=sheets-cf",
    "build": "node scripts/build.js",
    "deploy": "bash deploy.sh"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
